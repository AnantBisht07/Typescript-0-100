// Union --> with htis we can define that it can be of 2 types.
function combine(num1, num2) {
    return;
}
combine(20, 20);
combine("anio", false);
// literal Types --> we make our own type
function hello(a, conversionType) {
    return;
}
hello(10, "as-number");
function greet(person) {
    return;
}
greet(12);
var user = {
    name: "huoi",
    age: 21
};
